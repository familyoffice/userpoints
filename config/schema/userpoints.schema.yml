userpoints.settings:
  type: config_object
  mapping:
    userpoints_ui_bundles:
      type: sequence
      label: 'Entity types that have userpoints UI tab enabled'
      sequence:
        type: sequence
        label: 'Entity type bundles that have userpoints UI tab enabled'
        sequence:
          type: boolean
